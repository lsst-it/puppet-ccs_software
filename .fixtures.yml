# This file can be used to install module dependencies for unit testing
# See https://github.com/puppetlabs/puppetlabs_spec_helper#using-fixtures for details
---
fixtures:
  forge_modules:
    stdlib: "puppetlabs/stdlib"
    vcsrepo: "puppetlabs/vcsrepo"
    systemd: "camptocamp/systemd"
    sudo: "saz/sudo"
    accounts: "puppetlabs/accounts"  # beaker only
    yum: "puppet/yum" # java_artisanal dep
    alternatives: "puppet/alternatives" # java_artisanal dep
  repositories:
    java_artisanal: # beaker only
      repo: "git://github.com/lsst-it/puppet-java_artisanal"
      ref: "v1.0.0"
